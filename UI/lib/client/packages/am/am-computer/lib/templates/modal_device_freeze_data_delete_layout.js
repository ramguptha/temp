define([
  'ember',
  'text!../templates/modal_device_freeze_data_delete_layout.handlebars'
], function(
    Em,
    template
    ) {
  'use strict';

  return Em.Handlebars.compile(template);
});
