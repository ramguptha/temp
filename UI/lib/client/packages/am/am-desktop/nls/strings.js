define({
  root: {
    amDesktop: {
      shared: {
        copy: 'copy',
        invalidImageFormatMessage: 'Invalid image format'
      }
    }
  },

  // Supported Locales
  // -----------------

  'ja': true
});
