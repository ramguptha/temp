<a href="#" class={{view.tab.itemClass}} {{action "gotoTab" view.tab.item}}>
  {{#if view.tab.iconClass}}
    <span class="icon-font-size-xl {{view.tab.iconClass}}"></span>
  {{/if}}
  {{view.label}}
</a>
